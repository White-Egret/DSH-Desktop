# 固定 registry 为 **https**（安全审查 L-1）。
#
# package-lock.json 里的 resolved URL 由生成时的 registry 决定：如果照默认或
# 照 http:// 的镜像生成，全部 12 条 resolved 都会是明文 http://，CI 的 npm ci
# 也会照着走明文（哈希 integrity 能兜住内容被篡改，但安装行为本身在网络上明文可见）。
# 在这里显式写死 https，`npm install` / `npm ci` 都从 https 取包，
# 重新生成锁文件时也不会再退回 http。
#
# 换回官方源：把下一行改成 https://registry.npmjs.org/ 后重新 npm install。
registry=https://mirrors.tencent.com/npm/
